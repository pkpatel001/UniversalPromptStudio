"""Explicitly activated entry point for {{ values.plugin_id }}."""

from __future__ import annotations

from Engineering.PluginSystem import PluginRegistrationContext


class {{ values.class_name }}:
    """Trusted in-process plugin entry point."""

    def activate(self, context: PluginRegistrationContext) -> None:
        """Register manifest-declared contributions through context."""

    def deactivate(self, context: PluginRegistrationContext) -> None:
        """Release plugin-owned resources before deterministic host cleanup."""
