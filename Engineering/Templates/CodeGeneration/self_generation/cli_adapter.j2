"""Passive CLI adapter placeholder for {{ values.display_name }}."""

COMMAND_NAME = "{{ values.module_name.replace('_', '-') }}"


def register_cli_adapter() -> None:
    """Reserved for explicit host wiring after human review."""
