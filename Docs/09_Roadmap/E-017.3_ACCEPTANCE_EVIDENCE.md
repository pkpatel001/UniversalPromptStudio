# E-017.3 acceptance evidence

**Date:** 2026-08-24
**Clean baseline:** `0ac3d44532a3b5213dbe13dd25030b41153b9eef`
**Baseline parity:** local `HEAD`, `origin/main`, and live GitHub `main` matched

## Regression

| Gate | Fresh result |
| --- | --- |
| Python | 841 passed, 1 skipped |
| Expected skip | Plugin-runtime symlink creation unavailable on this Windows host |
| Frontend | 19 passed, 0 failed |
| Vite production build | Passed |
| `cargo check --locked` | Passed |

## Toolkit evidence

| Gate | Fresh result |
| --- | --- |
| E-010 full build | 4 succeeded, 0 failed |
| Python syntax inventory | 208 source files validated |
| Backend inventory | 26 source files inventoried |
| Manifest validation | 4 valid, 0 issues |
| Manifest migration planning | 1 non-writing plan, 1 step, 0 issues |
| Theme catalog drift | Current; 3 selections transported |
| Template definitions | 7 discovered |
| Self-generation dry-run | Ready; 10/10 preconditions, 5 artifacts, no writes |
| Self-generation drift cases | Passed within the full regression suite |
| Existing release verification | 4 artifacts independently verified |

The clean-baseline `release run --dry-run --overwrite` passed before edits. The
same command after documentation edits rejects only `git.dirty`, which is the
required release safety behavior for an uncommitted checkpoint.

## Package and version evidence

- Built one sdist and one wheel from current source with `python -m build
  --no-isolation` in a verified temporary directory.
- Inspected both archives successfully.
- Confirmed the wheel contains both Engineering subsystem definitions and the
  packaged self-generation Jinja assets.
- Confirmed Python, npm, and Cargo metadata agree on `0.2.0-alpha` (normalized
  to `0.2.0a0` in Python artifact filenames).
- Removed temporary package output after inspection.

No dependency was installed, no registry or provider was contacted, and no
commit, push, tag, release, signature, publication, or repository-generated
subsystem write was performed by E-017.3.
