# A-001.1 Acceptance Evidence

**Checkpoint:** A-001.1 — Explicit desktop-to-Python IPC foundation  
**Status:** Complete  
**Validated:** 2026-08-24

## Delivered boundary

- One Tauri-owned, long-lived Python application process in development builds.
- One versioned JSON-lines request/response protocol with bounded messages and correlated request IDs.
- One closed command: `application.readiness`.
- One frontend readiness action with pending, ready, and unavailable states.
- Structured, bounded failures with no stack traces or arbitrary backend values crossing the desktop boundary.
- Release builds fail closed until A-001.2 supplies the bundled Python sidecar.

## Acceptance results

- Python: `855 passed, 1 skipped` across the full repository suite.
- Frontend: `25 passed`; production Vite build succeeded.
- Rust: `3 passed`; formatting and locked release checks succeeded.
- Strict typing: no issues in the six new Python IPC source files.
- Scoped lint: no new findings after excluding the repository-wide uppercase `Backend` package-name rule.
- Engineering manifests: 4 valid, 0 issues.
- Manifest migrations: 1 deterministic plan, 1 step, 0 issues.
- Theme transport: generated catalog current with 3 selections.
- Engineering build: 4 succeeded, 0 skipped, 0 failed.

## Deferred to A-001.2

- Bundle Python as an installed desktop sidecar.
- Replace the development interpreter launch with packaged sidecar resolution.
- Add installed startup, shutdown, upgrade, and recovery verification.
- Keep the A-001.1 protocol and frontend command surface closed unless an accepted roadmap change expands them.
